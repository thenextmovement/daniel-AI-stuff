# RIESENOBJEKTE Missing-Attachment Auto-Reply

The active first-party form intake keeps its deterministic six-minute wait and exact
idempotency lock. The normal acknowledgement remains unchanged when at least one file
was uploaded or the project text says that no design exists, requests design work, or
supplies the intended wording.

Only an accepted real form submission with zero binary files and none of those text
exceptions receives the fixed missing-design reply. Customer input selects only the
reply kind; recipient, subject, body, sender and RIESENOBJEKTE signature remain fixed.
The project description is never copied into the customer email and the customer name
is HTML-escaped.

## Rollback

Deactivate `1hRkUxPXUZoYRSgL`, restore its nodes and connections from inactive exact
backup `7BDTyn3zmGIj72Ub`, validate the restored graph, then publish and reactivate it.
The change adds no database migration and does not touch NEONTRIP.
