{
  "name": "neontrip-lp-guard",
  "private": true,
  "version": "1.0.0",
  "description": "Playwright-based build-time guard for NEONTRIP landing pages. Catches regressions before deploy.",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
